* To count the number of documents in the cluster
  * ```{"query": { "match_all": {} }}```
* To count the number of documents in the cluster
  * ```{    "query" : {    "match" : {    "last_name" : "Smith"    }    }}```
* To count the number of documents in the cluster
  * ```{"query": { "match_all": {} }}```
* To count the number of documents in the cluster
  * ```{"query": { "match_all": {} }}```

