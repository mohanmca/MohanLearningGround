/open PRINTING
/** Integer to Binary **/
public String  ib(int n){
    return Integer.toBinaryString(n);
}
/** print integer to binary **/
public void  pib(int n){
    print(ib(n));
}
pib(1<<2);
pib(~(1<<2));
var x = 5;
pib( x );
pib( x ^ (x-1) );
print((x & 1) == 0)
